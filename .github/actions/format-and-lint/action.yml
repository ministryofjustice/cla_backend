name: Format and lint
runs:
  using: composite
  steps:

      - name: Requirements
        shell: bash
        run: python -m pip install -r requirements/generated/requirements-lint.txt

      # Lint using flake8
      - name: Linting
        shell: bash
        run: python -m flake8



